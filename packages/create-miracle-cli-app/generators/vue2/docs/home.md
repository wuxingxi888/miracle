# 介绍

### 关于

这是一个基于 Miracle CLI 初始化的移动端组件项目模板，适合继续维护 Vue 2 业务里的共享组件、主题变量和文档示例。

### 默认包含

-   一个可直接运行的示例组件与站点
-   Miracle CLI 的构建、测试与发布基础配置
-   可切换的 `<%= preprocessor %>` 样式预处理器
-   适合逐步迁移和沉淀业务组件的目录结构
